# Trader · 定版（飞书 Cron announce）

> 历史文件名保留；投递通道为 **飞书 SonicTeamNo.1**，不再使用 Telegram。

## 纪律

1. **全程仅一条**可见回复（最后一轮）；工具轮正文**零字**（只发 tool_calls）
2. 禁止 `message`/`send`、聊天里提文件路径
3. **禁止英文过程句（硬规则）**：`Let me start the pre-market routine` / `Now I'll run the search queries` / `in parallel` / `Let me check the available skills` / `SKILL.md` / `mx-search script path` / `Now I have enough data to compose` / `Let me write`，以及 `Now let me` / `Let me fetch` / `Good, got` / `compose and write` 等；**禁止**中文「让我先拉行情/接下来写报告/盘前流程」
4. **禁止** read `~/.openclaw/data/skills/**/SKILL.md`；妙想只按 **`MIAOXIANG.md`** exec
5. 拉指数、板块、自选、mx-search **只能在工具轮静默完成**；飞书只见中文定版
6. 长文先 **write** 本地 `reports/`，飞书只发**压缩定版**
7. 休市日：最后一轮仅 `NO_REPLY`

## 篇幅

- 单条飞书消息建议 ≤ 35 行；自选过多时列重点 5～8 只 +「其余见本地报告」
